{
  "name": "bullshitserver",
  "version": "1.0.0",
  "description": "A server made for fetching data to a code challenge.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd caesarcipher && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "author": "Jyrki Riestola",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
